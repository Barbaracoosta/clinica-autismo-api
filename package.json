{
  "name": "clinica-autismo-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT",
    "dev": "json-server db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}